# ADR 002 No Global Score

## Decision

Report independent domain findings and counts. Do not calculate a composite readiness score.

## Why

The BRD explicitly rejects a single opaque score because averages can hide a catastrophic transaction or authorization weakness and invite benchmark gaming.
